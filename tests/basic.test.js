import { expect, test } from 'vitest'

test('Math works', () => {
  expect(1 + 1).toBe(2)
})

test('Project name is correct', () => {
  const name = 'bodakitaa-fe'
  expect(name).toBe('bodakitaa-fe')
})
